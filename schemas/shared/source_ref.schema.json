{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "title": "source_ref",
    "description": "Points to a specific quote-backed line span in a canon scene.",
    "type": "object",
    "required": [
        "scene_id",
        "line_start",
        "line_end"
    ],
    "properties": {
        "scene_id": {
            "type": "string",
            "pattern": "^[0-9]{2}-[0-9]{2}-[0-9]{2}$",
            "description": "Scene identifier in BB-CC-SS format (Book, Chapter, Scene)"
        },
        "line_start": {
            "type": "integer",
            "minimum": 1,
            "description": "First line number of the quoted span"
        },
        "line_end": {
            "type": "integer",
            "minimum": 1,
            "description": "Last line number of the quoted span"
        },
        "quote": {
            "type": "string",
            "description": "Optional direct quote text for context"
        }
    },
    "additionalProperties": false
}